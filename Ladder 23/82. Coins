#include <bits/stdc++.h>
using namespace std;
#define int long long int

int32_t main() {
    int n;
    cin>>n;
    
    for(int i=n;i>=1;i--){
        if(n%i == 0){
            n = i;
            cout<<i<<" ";
        }
    }
    
    return 0;
}
